创建虚拟环境(推荐):

cd ./daily_stock_analysis

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
.\venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt


# 仅启动 WebUI， 手动触发分析
python main.py --webui-only